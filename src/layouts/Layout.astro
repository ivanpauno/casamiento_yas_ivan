<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<!-- <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" /> -->
		<meta name="generator" content={Astro.generator} />
		<title>Nos casamos — Yasmin & Ivan</title>
		<style>
			:root {
				--bg: #cfdbcd;
				--bg-gradient: linear-gradient(180deg, rgba(207, 219, 205, 0.92) 0%, rgba(242, 232, 216, 0.95) 100%);
				--card-bg: #ebdec7;
				--card-gradient: linear-gradient(180deg, rgba(235, 222, 199, 0.95) 0%, rgba(249, 241, 225, 0.98) 100%);
				--action-bg: #f1ca87;
				--text: #111827;
				--border: rgba(0, 0, 0, 0.15);
				--radius: 12px;
				--section-gap: clamp(10px, 2vw, 30px);
				--page-padding: clamp(10px, 2vw, 30px);
				--font-body: 'Montserrat', sans-serif;
				--font-heading: 'ZapfChanceryStd-Light';
				--font-script: 'ZapfChanceryStd-Light';
			}
			@font-face {
				font-family: 'ZapfChanceryStd-Light';
				src: url('/fonts/ZapfChanceryStd-Light.ttf') format('truetype');
				font-weight: 10;
				font-style: normal;
				font-display: swap;
			}
			@font-face {
				font-family: 'Montserrat';
				src: url('/fonts/Montserrat-VariableFont_wght.ttf') format('truetype');
				font-weight: 330;
				font-style: normal;
				font-display: swap;
			}
			html, body {
				margin: 0;
				min-height: 100vh;
				font-family: var(--font-body);
				background: var(--bg-gradient);
				color: var(--text);
				-webkit-font-smoothing: antialiased;
				min-width: 0;
				overflow-x: hidden;
			}
			/* ensure predictable box-sizing and prevent grid items from forcing overflow */
			*, *::before, *::after { box-sizing: border-box; }
			.site { max-width: none; width: 100%; margin: 0; padding: 0; display: grid; gap: 10px 5px; }
			.site > * { width: 100%; min-width: 0; }
			:global(h1) {
				font-family: var(--font-heading);
				color: #545454;
			}
		</style>
	</head>
	<body>
		<div class="site">
			<slot />
		</div>
	</body>
</html>
