---
const { title = 'Dress-code', description = 'Formal — traje oscuro / vestido de cóctel' } = Astro.props;
---
<div class="card">
  <h2 style="margin:0 0 8px 0">{title}</h2>
  <p style="margin:0;color:var(--muted)">{description}</p>
</div>
